[project]
name = "ocotillopmf-examples"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "cmasher>=1.9.2",
    "jupyterlab>=4.6.4",
    "matplotlib>=3.11.2",
    "ocotillopmf",
    "seaborn>=0.13.2",
]

[tool.uv.sources]
ocotillopmf = { path = "../", editable = true }
